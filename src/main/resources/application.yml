server:
  port: 8090

spring:
  application:
    name: user

  datasource:
    url: jdbc:postgresql://localhost:5432/Users
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
